import * as PDFDocument from 'pdfkit';

class Render {
  private images: Array<string>;

  constructor(files: Array<string>) {
    this.images = files;
  }
}
export function render(images: Array<string>) {

};
